let squaredNumber = num => num * num; // could also use the following formula : return num ** 2; 
                                      // ** is called an exponential operator 

console.log(squaredNumber(3)); // 9