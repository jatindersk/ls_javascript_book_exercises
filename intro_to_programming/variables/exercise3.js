// {
//   let foo = 'bar';
// }

// console.log(foo);

// We will get 'foo' not defined error. This is because, foo is declared using let keyword which is block scoped and so foo doesn't exist outside the curly braces.
// Hence, not defined error message gets generated.
